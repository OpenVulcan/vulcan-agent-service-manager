# Vulcan Agent Service Manager

`vasm` 是 Vulcan Agent Service 的独立安装器与终端管理器。本仓库独立编译和发布管理器；主程序及其运行资源仍由 `vulcan-agent-service` 仓库发布。

完整功能与跨仓库契约见[设计方案](docs/design/vasm-manager-design.md)。当前仓库尚未实现安装器程序。
